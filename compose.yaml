services:
  feet-bot:
    build: .
    image: feet-bot
    container_name: feet-bot
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env:ro
      - feet-data:/app/data
volumes:
  feet-data:
    name: feet-data